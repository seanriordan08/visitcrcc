<% if !signed_in? %>
  <%= render partial: 'sign_up_form' %>
  <%= render partial: 'log_in_form' %>
<% end %>

<%= render partial: "declaration_modals" %>
<%= render partial: "mission_modal" %>

<% if signed_in? %>
  <%= render partial: 'modal_edit_user'%>
<% end %>
